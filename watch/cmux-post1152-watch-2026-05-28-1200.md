# cmux post-11:52 watch, 2026-05-28 12:00 UTC

Tracker: [#52](https://github.com/serejaris/corp-opensource/issues/52)

Обязательные skills `open-source-bug-scouting` / `open-source-pr-workflow` в этом окружении всё ещё не установлены. Цикл выполнен через documented fallback: 6 read-only subagent ролей, parent live gates и focused 3-role critique.

Upstream comment, PR, ping, rerun или runner action не выполнялись.

`next_status: CANDIDATE`

Fresh delta: `manaflow-ai/cmux` = repo-scope `WATCH`.

Upstream action count: `0`.

Runner action count: `0`.

## Parent live gates

Runner gate остаётся закрытым через [#10](https://github.com/serejaris/corp-opensource/issues/10): dedicated `corp-opensource-runner` не provisioned. CT `216` остаётся Hermes-only и не использовался для general repro.

| Lane | Live result | Decision |
|---|---|---|
| `manaflow-ai/cmux` | Новый repo-scope сигнал. На момент live gate: public, non-archived JavaScript repo; license `Other`; `20,074` stars, `1,508` forks, pushed `2026-05-28 11:57:33 UTC`. Repo metadata positions it as a Ghostty-based macOS terminal/control surface with vertical tabs and notifications for AI coding agents. Topics include `claude-code`, `codex`, `gemini`, `ghostty`, `opencode`, `terminal`, `tmux`. Local README/watch search found no prior `cmux`/`manaflow` entry. | Repo-scope `WATCH`; high-fit terminal/control-plane surface, but no bounded issue, source scan, repro, contribution/process pass, or issue-level duplicate/race gate. |
| `manaflow-ai/cmux#4941` | Open, unassigned, no labels/comments. Remote SSH Claude Code session discovery for Vault/Feed. | `WATCH`; useful terminal/session signal, not a proven regression lane. |
| `manaflow-ai/cmux#4938/#4939` | `#4938` is open and unassigned: `claude --resume` binding missing for same-cwd or dot-path worktrees. Open PR `#4939` touches `RestorableAgentSession.swift`, `SessionIndexStore.swift`, and tests for dot-path/worktree resume binding; it is blocked. | `WATCH/NO-GO for #4938-like path`; adjacent/partial cover exists, do not race. |
| `manaflow-ai/cmux#4944` | Fresh open PR updated `2026-05-28 11:58:03 UTC`: `Guard E2E Depot runner identity`, `CHANGES_REQUESTED`/blocked. | `WATCH`; churn/process signal only. |
| `anthropics/claude-code#63104/#63105` | Fresh post-cutoff product issues: `#63104` `/context` MCP/TUI context blow-up has `has repro` but duplicate bot linked `#61907/#62750/#18844`; `#63105` is a feature request for precache expiry hook. | Product/support `WATCH`; no source-local public patch lane. |
| `vercel/ai#15652/#15668/#15669` | `#15652` and `#15668` remain open/unassigned with prior comments, no post-cutoff transition. Draft PR `#15669` is about stream accumulation, not an exact cover for `#15668`. | Carry-forward `CANDIDATE`/`LEAD`, still runner/package-fixture gated; no upstream action. |

Carry-forward backlog stayed open and uncovered but did not transition: `probe#568`, `claude-peers-mcp#64`, `google-gemini/gemini-cli#27503`, `vercel/ai#15652`, `simular-ai/Agent-S#195`, `browseros-ai/BrowserOS#1005`, `generalaction/emdash#1875`, `topoteretes/cognee#2907`, `agentmemory#703-#708`, `OpenViking#2256`, `openai/codex#24903/#24904`, `claude-code#63103`, and `opencode#29730`.

## 6-role synthesis

- Fresh discovery resurfaced `vercel/ai#15652` as the best existing candidate and noted `vercel/ai#15668`, LiteLLM `#29142`, CUA `#1739`, Codex `#24904`, and opencode `#29730`, but none changed the current upstream action gate.
- Active refresh found no post-cutoff state/comment/assignee/label transition for the tracked backlog.
- Duplicate/race guard found no lane that can move above `WATCH/CANDIDATE`; `opencode#29730` is assigned and duplicate-clustered, Codex/Claude items are product surfaces, and `OpenViking`/`cognee` remain source-validation leads.
- Process gates confirmed upstream comment/PR is not allowed now: runner is unavailable, `gemini-cli#27503` lacks the expected help-wanted/community assignment gate, Codex external PRs are invitation-only, and candidate lanes lack fail-before evidence.
- Actionability kept no-runner source-first order as `claude-peers-mcp#64`, `Agent-S#195`, partial `gemini-cli#27503`, then OpenViking/cognee source passes; true runner order starts with `probe#568`.
- Scope/materiality selected `manaflow-ai/cmux` as the only new repo-scope material delta after the 11:52 watch. `open-design` was already recorded; opencode/Hermes/Codex are already covered in existing context.

## 3-role critique

- Factology/duplicates: record `cmux` only as repo-scope `WATCH`; star/fork counts are live-gate snapshots, license is just GitHub `Other`, and repo metadata was not source-verified. `#4941` and `#4938` are not candidate bugs. PR `#4939` is adjacent/partial cover for the `#4938`-like resume binding path, and `#4944` is only churn/process signal.
- Process gates: mandatory-skill fallback is a process risk, not a full substitute. Internal watch/tracker update is allowed; upstream action remains blocked by missing runner/source evidence and no selected bounded `cmux` lane.
- Actionability: keep `cmux` separate from the candidate queue. For `cmux`, the next useful step is a future focused pass that chooses exactly one reproducible terminal/session/control-plane issue and then runs source/process/duplicate gates.

## Next

Keep `manaflow-ai/cmux` on repo-scope watch. Do not open or comment upstream until a future cycle selects one bounded issue, verifies source ownership and contribution process, repeats issue/PR duplicate search, and has runner or source-level fail-before evidence. Existing no-runner source-first candidates remain `claude-peers-mcp#64`, `Agent-S#195`, and partial `gemini-cli#27503`; the first full dedicated-runner target remains `probe#568`.
